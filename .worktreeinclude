# Gitignored files copied into every new Claude Code worktree.
# Gitignore syntax. See https://code.claude.com/docs/en/worktrees

# Per-user permission state — absent from a clean checkout, so a fresh worktree
# would otherwise re-prompt for every already-approved tool call.
.claude/settings.local.json

# Deliberately NOT listed: node_modules. A bun tree carries symlinks and binaries
# that do not survive a copy intact, and a corrupted one fails `verify` in ways that
# read like real code errors. backlog-deliver runs an explicit `bun install` instead.
